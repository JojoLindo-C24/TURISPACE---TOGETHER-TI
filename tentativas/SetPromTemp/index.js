import {SetPromTemp} from './styled'

export default function IndexSetPromTemp(props){
    return(
        <SetPromTemp>
            <div className="titulo">{props.title}</div>
        </SetPromTemp>
    )
}